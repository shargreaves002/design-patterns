package behavioral.strategy;

public class QuickSortingStrategy implements SortStrategy {
    @Override
    public int[] sort(int[] arr) {
        System.out.println("Sort using quick sort");
        return new int[0];
    }
}
